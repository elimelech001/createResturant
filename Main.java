import java.util.ArrayList;

public class Main {

    // 1) need to offer a portion to every costomer;
    // 1.1) build a array of all portions
    // 1.2) show costomer the portions
    // 1.3) pick a portion
    // 1.4) save it somwhere

    // 2) need to asign a waiter for every table;
    // 2.1) get waiters from shift;
    // 2.2) asign in each table and save it by table

    // 3) need to asign a bill for every table;
    // 3.1) check price of orded portion;
    // 3.2) save a bill on the table;
    // 3.3) when costomer leaves clear bill

    // 4) need to offer a recipt for every table
    // 4.1) show the bill;
   public static void main(String[] args) {
    Resturant resturant = new Resturant();
    
    
   }
}
